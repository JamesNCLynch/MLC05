@model IEnumerable<MLC05.Models.ScheduledClass>

@{
    ViewBag.Title = "Upcoming classes";
}

<h2>Upcoming classes that need to be completed</h2>

@* <p> *@
@*     @Html.ActionLink("Create New", "Create") *@
@* </p> *@
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ClassStartTime)
        </th>
        <th>
            Class type
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ClassStartTime)
             at 
            @Html.DisplayFor(modelItem => item.ClassStartTime.TimeOfDay)
        </td>
        <td>
            @Html.DisplayFor(m => item.ScheduledClassType.Name)
        </td>
        <td>
            @Html.ActionLink("Create New", "Create", new {type = item.ScheduledClassType.Name, start = item.ClassStartTime})
            @* @Html.ActionLink("Edit", "Edit", new { id = item.Id }) | *@
            @* @Html.ActionLink("Details", "Details", new { id = item.Id }) | *@
            @* @Html.ActionLink("Delete", "Delete", new { id = item.Id }) *@
        </td>
    </tr>
}

</table>
